<UserControl
    x:Class="SimplePad.Windowing.UWP.Views.ShellView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="using:SimplePad.Windowing.UWP.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:multiTab="using:SimplePad.MultiTab.UWP.Controls"
    xmlns:settings="using:SimplePad.Settings.UWP.Views"
    xmlns:using="using:SimplePad.Themes.UWP.Controls"
    xmlns:winui="using:Microsoft.UI.Xaml.Controls"
    d:DesignHeight="300"
    d:DesignWidth="400"
    winui:BackdropMaterial.ApplyToRootOrPageBackground="True"
    mc:Ignorable="d">
    <using:ThemeContainer>
        <Grid>
            <multiTab:AppTabView>
                <multiTab:AppTabView.TitleBar>
                    <Border x:Name="TitleBar" Background="Transparent" />
                </multiTab:AppTabView.TitleBar>
                <multiTab:AppTabView.TabStripHeader>
                    <Image
                        Width="28"
                        Height="28"
                        Margin="9,0,5,0"
                        IsHitTestVisible="False"
                        Source="/Assets/Square44x44Logo.png" />
                </multiTab:AppTabView.TabStripHeader>
                <multiTab:AppTabView.TabStripFooter>
                    <Border Width="192" />
                </multiTab:AppTabView.TabStripFooter>
            </multiTab:AppTabView>
            <settings:SettingsView x:Name="SettingsView" />
        </Grid>
    </using:ThemeContainer>
</UserControl>